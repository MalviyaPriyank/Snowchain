   CREATE OR REPLACE SCHEMA SQL_SERVER_CDC_DB.CDC_LANDING  DATA_RETENTION_TIME_IN_DAYS = 1  REPLACE_INVALID_CHARACTERS = FALSE  COMMENT = "Schema for landing SQL Server CDC data before downstream processing" 