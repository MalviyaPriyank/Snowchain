CREATE OR REPLACE TABLE RETAIL_DB.SOURCE_SCHEMA.CATEGORIES ( category_id INTEGER PRIMARY KEY,  category_name VARCHAR(50),  description VARCHAR(200),  last_modified TIMESTAMP DEFAULT CURRENT_TIMESTAMP()  ) 