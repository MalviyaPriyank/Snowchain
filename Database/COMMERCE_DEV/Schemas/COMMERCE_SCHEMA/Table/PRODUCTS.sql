CREATE OR REPLACE TABLE commerce_dev.COMMERCE_SCHEMA.PRODUCTS ( product_id INTEGER PRIMARY KEY,  product_name VARCHAR(100),  category_id INTEGER,  unit_price DECIMAL(10,2),  stock_quantity INTEGER  ) 